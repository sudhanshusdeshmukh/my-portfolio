/**
 * Tailwind CSS v4 works zero-config with the Vite plugin (`@tailwindcss/vite`).
 * This optional config is provided for future customization (themes, tokens, etc.).
 * See: https://tailwindcss.com/docs/configuration
 */

export default {
  // Add theme/extensions here when needed, e.g.:
  // theme: {
  //   extend: {
  //     colors: { brand: '#1a391d' },
  //   },
  // },
}
